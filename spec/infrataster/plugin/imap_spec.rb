require 'spec_helper'

describe Infrataster::Plugin::Imap do
  it 'has a version number' do
    expect(Infrataster::Plugin::Imap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
